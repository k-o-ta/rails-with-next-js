{
  "name": "rails-with-next-js",
  "engines": {
    "node": "16.x"
  },
  "scripts": {
    "postinstall": "cd ./frontend && npm install && npm run build"
  },
  "dependencies": {
    "next": "^12.1.0"
  }
}
