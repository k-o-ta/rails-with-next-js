{
  "name": "rails-with-next-js",
  "engines": {
    "node": "16.x"
  },
  "scripts": {
    "postinstall": "npm --prefix ./frontend install ./frontend && npm --prefix ./frontend run build"
  },
  "dependencies": {
    "next": "^12.1.0"
  }
}
